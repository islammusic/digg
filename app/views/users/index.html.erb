<% @users.each do |u|%>
	<%= u.id %>
	<%= u.admin %>
	<%= u.name %> 
	<%= u.surname %> 
	(<%= u.email %>)
	<% if admin? %>
		<%= link_to "Izbriši", u, 	:method => :delete, 
									:confirm => "A si ziher?"%>
	<% end %>
	<% if correct_user?(u) %>
		<%= link_to "Uredi", edit_user_path(u)%>
	<% end %>
	<br />
<% end %>